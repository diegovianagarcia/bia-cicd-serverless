version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
  build:
    commands:
      - echo "Executando react build"
      - cd client
      - npm install
      - NODE_OPTIONS=--openssl-legacy-provider REACT_APP_API_URL=https://t1jtl2ca96.execute-api.us-east-1.amazonaws.com/Prod SKIP_PREFLIGHT_CHECK=true npm run build

artifacts:
  files:
    - "**/*"
  base-directory: client/build
